const express = require('express');
const alumnosRoutes = require('./routes/alumnosRoutes');
const routes = require('../routes/maestrosRoutes');
const app = express();

// Configurar body-parser para
